<div class='container'>
  <h1>new.html.erb</h1>
  <div>
    <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content %>
      <%= f.input :rating %>
      <%= f.submit "add a review", class: "btn btn-primary" %>
    <% end %>
  </div>
  <div>
    <%= link_to 'Nos Restaurants', restaurants_path %>
  </div>
</div>
